@using Microsoft.AspNetCore.Identity
@using BeanSceneDipAssT2.Areas.Identity.Data

@inject SignInManager<AppUser> SignInManager
@inject UserManager<AppUser> UserManager

@{
    ViewData["Title"] = "Done";
}

<div class="container">
    <div class="row">
        <div class="col-md-3">
            <br />
            <h2>This Is Our Website!</h2>
            <dl>
                <dt><a href="/Home/Menu" class="btn">Food Menu</a></dt>
                <dd>We have based on Mexican food!</dd>
            </dl>
        </div>
        <div class="col-md-6">
            <h2>
                Successfully made it Reservation!
                Please wait for progress your Booking!
            </h2>

            <a class="btn btn-d" asp-area="" asp-controller="Home" asp-action="Index">
                Back to HomePage
            </a>
            <br>

        </div>
        @if (SignInManager.IsSignedIn(User))
        {
            <div class="col-md-3">
                <a class="btn" asp-area="Identity" asp-page="/Account/Manage/ReservationsHistory">
                   Reservations History
                </a>
            </div>
            }
        </div>
</div>